import _ from "lodash";

const max = _.max([2, 4, 5, 9]);

console.log(max);

// Example 1:

let s = "Hello World";

let counter = 0;
var lengthOfLastWord = function (s) {
  for (i = s.length - 1; i >= 0; i--) {}
};

console.log(s[i]);
